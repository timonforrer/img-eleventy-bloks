{
  "name": "img-eleventy-bloks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist prebuild",
    "build": "npm run clean && npx @11ty/eleventy",
    "dev": "npm run clean && NODE_ENV=debug netlify dev:exec npx @11ty/eleventy --serve"
  },
  "keywords": [],
  "author": "Timon Forrer",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0-canary.42",
    "@11ty/eleventy-cache-assets": "^2.3.0",
    "fast-glob": "^3.2.7",
    "rimraf": "^3.0.2",
    "slugify": "^1.6.0"
  },
  "dependencies": {
    "chrome-aws-lambda": "^5.5.0",
    "puppeteer": "^5.5.0",
    "puppeteer-core": "^5.5.0",
  }
}
